function enter(pi) {
        pi.playerMessage(5, "The cabin is closed off due to the eradication of Balrogs.");  // Displays a message to the player
        return false;  // Prevents the player from entering
    }
