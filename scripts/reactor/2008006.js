/*
	名字:	隱密之地
	地圖:	雅典娜禁地&amp;lt;休息室&gt;
	描述:	920010400
*/

function act() {
	rm.getPlayer().getMap().broadcastMessage(Packages.tools.packet.CWvsContext.serverNotice(5, "The music played through the air."));
}